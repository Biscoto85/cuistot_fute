# URL de l'API en développement (proxy Vite, pas besoin de changer)
# Le proxy /api -> http://localhost:3003 est configuré dans vite.config.ts
# En production, Nginx gère le proxy — cette variable n'est pas utilisée.
VITE_API_BASE_URL=http://localhost:3003
